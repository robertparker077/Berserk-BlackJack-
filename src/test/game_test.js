// import checkForAce from './lib/hand'

describe("checkForAce", () => {
  it("Checks for a ace in a given hand of two cards", () => {
    // expect(checkForAce(Dealer.hand.[Card {suit: {name: 'Club', symbol: '♣️'},
    //                                       type: {name: 'Ace', value [1,11]} })).
    // to.equal(true)

  })
})
